// ===== ENUMS =====

export enum Rarity {
    COMMON = 'common',
    RARE = 'rare',
    EPIC = 'epic'
}

export enum Condition {
    NEW = 'new',
    USED = 'used',
    DAMAGED = 'damaged'
}

export enum ItemCategory {
    WEAPON = 'weapon',
    TECH = 'tech',
    ARTIFACT = 'artifact',
    CONSUMABLE = 'consumable',
    WEARABLE = 'wearable'
}

export enum WearableSlot {
    HEAD = 'head',
    BODY = 'body',
    ACCESSORY = 'accessory'
}
